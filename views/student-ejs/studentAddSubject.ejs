<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Add Subject to Student</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="/css/student-css/inputStudent.css" />
    <script src="main.js"></script>
</head>

<body>
    <form action="/students/<%= student.id%>/add-subject " class="form" method="POST">
        <h2>add Subject to Student</h2>

        <label for="your-name">subject</label> is
        <select name="SubjectId">
            <% for(let i = 0; i<subject.length;i++){  %>
                <option value="<%= subject[i].id %>">
                    <%= subject[i].subjectName %>
                </option>
                <% }%>
        </select>
        <button>
            <input type="submit" value='Add'>
        </button>
        <a href="/">Home</a>
    </form>

</body>

</html>